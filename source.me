#!/bin/bash

ROOT="$(git rev-parse --show-toplevel)"
PATH="${ROOT}:${PATH}"

alias update-to-zeus='git submodule foreach bash -c "git checkout zeus; git pull; true"'

cd "${ROOT}/poky" &> /dev/null
source oe-init-build-env ../build
